-- USAGI_filenames
SELECT DISTINCT FILENAME, replace(LEFT(FILENAME, CHARINDEX('.csv',FILENAME)-1),'-','_') AS TABLENAME
FROM  {{ source('OMOP_DEMO','OMOP_DEMO_STAGE_META')}} AS OMOP_DEMO_STAGE_META
ORDER BY FILENAME