version: 2

models:
  - name: ZC_VAL_TYPE_stg
    description: This table contains the category item for the flowsheet row value type.
    columns:
      - name: VAL_TYPE_C
        description: 	This is the flowsheet row's value type.
        tests:
          - unique #if primary key
          - not_null #if primary key
      - name: ZC_VAL_TYPE_NAME
        description: UPPER("NAME") The name given to this category item.

